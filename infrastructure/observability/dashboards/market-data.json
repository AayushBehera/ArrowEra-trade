{
  "annotations": { "list": [] },
  "editable": true,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "targets": [{ "expr": "market_data_latency_seconds", "legendFormat": "{{symbol}}" }],
      "title": "Market Data Latency",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [{ "expr": "rate(cache_hits_total[5m]) / (rate(cache_hits_total[5m]) + rate(cache_misses_total[5m]))", "legendFormat": "Hit Rate" }],
      "title": "Cache Hit Rate",
      "type": "gauge"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 8 },
      "id": 3,
      "targets": [{ "expr": "rate(market_data_requests_total[5m]) by (provider)", "legendFormat": "{{provider}}" }],
      "title": "Market Data Requests by Provider",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["arrowera", "market-data"],
  "title": "ArrowEra Market Data Quality",
  "uid": "arrowera-market-data"
}
